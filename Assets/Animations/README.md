# Animations

Current animations (squash & stretch, blink, springs, doors, UI pop-ins) are procedural and code driven
(`TrykliVisual`, mechanisms, `UITween`). Animator controllers for final art can be stored here.
