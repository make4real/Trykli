# Localization

Language files live in `Resources/Localization/<code>.json` (`fr.json`, `en.json`).
To add a language, copy `en.json`, change `code` / `displayName` and translate the values: it appears automatically
in the settings language selector.
