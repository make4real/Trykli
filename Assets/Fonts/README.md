# Fonts

The UI uses the TextMeshPro default font (LiberationSans SDF, imported with the TMP Essential Resources).
To use a custom font, create a TMP Font Asset and save it as `Resources/Fonts/TrykliFont.asset`.
