# Audio

No audio file is required: every sound hook (`SfxId`) falls back to a procedural placeholder sound.
To add real sounds, create **Assets > Create > TRYKLI > Audio Library**, save it as `Resources/TrykliAudioLibrary.asset`
and assign clips per `SfxId` plus the menu / gameplay music.
